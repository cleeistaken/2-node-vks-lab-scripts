#!/bin/bash

set -euo pipefail

# Load configuration
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CONFIG_FILE="${SCRIPT_DIR}/vks-lab.conf"

if [[ ! -f "$CONFIG_FILE" ]]; then
    echo "Error: Configuration file not found at $CONFIG_FILE"
    exit 1
fi

source "$CONFIG_FILE"

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m'

log_info() {
    echo -e "${GREEN}[INFO]${NC} $1"
}

log_warn() {
    echo -e "${YELLOW}[WARN]${NC} $1"
}

log_error() {
    echo -e "${RED}[ERROR]${NC} $1"
}

log_step() {
    echo -e "${BLUE}[STEP]${NC} $1"
}

# Check if a context already exists by parsing vcf context list output
context_exists() {
    local context="$1"
    # vcf context list output format may vary, so try to find the context name anywhere in the output
    vcf context list 2>/dev/null | grep -i "$context" > /dev/null 2>&1 && return 0 || return 1
}

# Create or refresh a context
create_or_refresh_context() {
    local context_name="$1"
    local endpoint="$2"
    local username="$3"
    local context_selector="$4"  # Format: "context:namespace" for vcf context use
    local extra_args="${5:-}"
    
    log_step "Creating context: $context_name"
    echo -e "${CYAN}When prompted, enter password:${NC} ${CYAN}$SUPERVISOR_PASSWORD${NC}"
    echo ""
    
    # Try to create the context - run directly to show prompts, use temp file for errors
    local error_file
    error_file=$(mktemp)
    
    vcf context create "$context_name" \
        --endpoint "$endpoint" \
        --username "$username" \
        $extra_args 2>"$error_file"
    local create_status=$?
    
    local create_error
    create_error=$(cat "$error_file")
    rm -f "$error_file"
    
    # Check if the error is "already exists"
    if [[ $create_status -ne 0 ]]; then
        if echo "$create_error" | grep -qi "already exists"; then
            echo ""
            log_info "Context already exists: $context_name"
            log_step "Refreshing context: $context_selector"
            
            # Refresh the specific context (no prompting)
            error_file=$(mktemp)
            vcf context refresh "$context_selector" 2>"$error_file"
            local refresh_status=$?
            local refresh_error=$(cat "$error_file")
            rm -f "$error_file"
            
            if [[ $refresh_status -eq 0 ]]; then
                log_info "Context refreshed successfully"
            else
                log_warn "Context refresh returned: $refresh_error"
            fi
        else
            echo ""
            log_error "Failed to create context: $create_error"
            return 1
        fi
    else
        echo ""
        log_info "Context created successfully"
    fi
    return 0
}

log_info "Logging into Supervisor Namespace: $SUPERVISOR_NAMESPACE_NAME"
echo ""

# Step 1: Create or refresh Supervisor Context
if ! create_or_refresh_context "$SUPERVISOR_CONTEXT" "$SUPERVISOR_IP" "$SUPERVISOR_USERNAME" \
    "$SUPERVISOR_CONTEXT:$SUPERVISOR_NAMESPACE_NAME"; then
    exit 1
fi

echo ""

# Step 2: Select Supervisor Context (if not already selected by create_or_refresh_context)
log_step "Ensuring context is selected: $SUPERVISOR_CONTEXT:$SUPERVISOR_NAMESPACE_NAME"
if vcf context use "$SUPERVISOR_CONTEXT:$SUPERVISOR_NAMESPACE_NAME" 2>/dev/null; then
    log_info "Supervisor context confirmed"
else
    log_error "Failed to select Supervisor context"
    exit 1
fi

echo ""

# Step 3: Validate connection
log_step "Validating connection to Supervisor..."
if kubectl get nodes > /dev/null 2>&1; then
    log_info "Connection validated successfully"
    echo ""
    log_step "Cluster nodes:"
    kubectl get nodes
else
    log_error "Failed to validate connection to Supervisor"
    exit 1
fi

echo ""
log_info "Successfully logged into Supervisor Namespace: $SUPERVISOR_NAMESPACE_NAME"
